package com.example.prachibodas.hellohaptics;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;
import com.senseg.haptics.Grain;

public class MainActivity extends AppCompatActivity {

    // prepare for trouble make it double (not float)

    // To protect the world from devastation
    // to unite all people within our nation
    // to denounce the evils of truth and love
    // to extend our reach to the stars above

    // prachi, bobby

    // team haptics

    // blast off with the speed of light
    // surrender or be prepared to fight

    // meowth! thats right!


    // ^DO NOT DELETE THE LINES ABOVE. CODE WILL BREAK IF DELETED.
    //                                                  -- Prachi. Bodas.

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);

        //Grain g = new Grain(Grain.GRAIN_AREA_BUMPY);
        //g.play();

    }
}
